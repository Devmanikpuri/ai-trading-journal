services:
  - type: web
    name: trading-journal-tracker
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: DB_PATH
        value: /data/trading-journal.db
    disk:
      name: trading-data
      mountPath: /data
      sizeGB: 1
